# Read file from SMB (samba) location using python
Read a file using Python implementation of the client-side SMB/CIFS protocol

## Pre-requisite:
    $ pip install pysmb

Edit the followinf in python file "smb-readfile.py" (provide values)